Commands

> conda install -c conda-forge --override-channels boto3
>
> python -m ipykernel install --user --name=myenv --display-name "Python (myenv)"


Ensure to get packages as mentioned in `requirements.txt` (pip)